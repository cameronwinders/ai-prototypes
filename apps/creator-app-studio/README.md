# Creator App Studio

This prototype lives at `apps/creator-app-studio` in the `ai-prototypes` monorepo.

## Factory Metadata

- app slug: `creator-app-studio`
- schema: `app_creator_app_studio`
- Vercel project: `ai-prototypes-creator-app-studio`

## Build Checklist

1. Paste the PM, Design, Testing, and Orchestrator briefs into `spec/`.
2. Run `npm install` at the monorepo root if the workspace lockfile changed.
3. Run `npm run verify:config --workspace=creator-app-studio`.
4. Run `npm run build --workspace=creator-app-studio`.
5. Deploy from Vercel using root directory `apps/creator-app-studio`.
